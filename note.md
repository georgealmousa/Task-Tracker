Q1: what is the difference between json.load and json.loads?

load() : it accepts a file object. To read JSON data from a file and convert it into a dictionary. Json. loads(): to convert JSON string to a dictionary


Q2: 

